# Medium 官方匯出（含個人隱私資料）
medium-export-*/
*.zip

# macOS
.DS_Store

# Claude Code
.claude/

# 讀者留言與回覆草稿（含讀者姓名）
QA.md
qa/

# 題目研究筆記與雜項工作檔（只留在本機）
backlog/
temp.txt
# 只擋根目錄的散落圖檔；story/ 底下的 .jpg 是文章插圖，不能擋
/*.jpg

# 去識別化樣式(含真實類名與路徑,只留本機)
tools/mcp/medium-check/deident-patterns.txt

# Python 快取
__pycache__/

# iThome 鐵人賽工作檔(只留本機)
# 大小寫全擋 —— 資料夾曾被建成 iTHome,而 Linux 上 git 比對是大小寫敏感的,
# 漏擋就會把夥伴姓名與未發布草稿推上這個公開 repo。
[iI][tT][hH][oO][mM][eE]/

# hook 與其自測:偏本機隱私設定,和 .claude/ 同一個理由不進版控
# (文章裡完整呈現內容;要讓讀者直接抄就把這兩行拿掉)
scripts/deident-guard.py
scripts/deident-guard-selftest.py
